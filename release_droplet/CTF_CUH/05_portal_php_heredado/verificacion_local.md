# Verificación local

Carpeta del reto: `05_portal_php_heredado`

## Comando principal
```powershell
cd 05_portal_php_heredado
python verify_organizer.py
```

## Resultado esperado
- El script debe devolver `OK`.
- La salida del validador debe incluir `CUH{portal_php_heredado_endurecido}`.
- El archivo `portal_php_heredado_bundle.zip` debe existir y abrir correctamente.

## Comprobaciones extra
- El bundle no debe incluir `solutions/`.
- El bundle sí debe incluir `README_ALUMNO.txt`.
- El material de apoyo publicado debe resolver en `/portal-php-heredado`.
