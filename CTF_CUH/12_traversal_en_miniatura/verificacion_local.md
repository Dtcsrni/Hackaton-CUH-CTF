# Verificación local

Carpeta del reto: `12_traversal_en_miniatura`

## Comando principal
```powershell
cd 12_traversal_en_miniatura
python verify_organizer.py
```

## Resultado esperado
- El script debe devolver `OK`.
- La salida del validador debe incluir `CUH{rutas_normalizadas_y_resueltas}`.
- El archivo `traversal_en_miniatura_bundle.zip` debe existir y abrir correctamente.

## Comprobaciones extra
- El bundle no debe incluir `solutions/`.
- El bundle sí debe incluir `README_ALUMNO.txt`.
- El material de apoyo publicado debe resolver en `/traversal-en-miniatura`.
